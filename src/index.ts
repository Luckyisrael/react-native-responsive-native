// Setup
export { setDesignSize } from './config';

// Scaling functions
export { wp, hp, s, f, rv, widthPercent, heightPercent, scale, font, responsiveValue } from './utils/scaling';

// Device detection
export { isPhone, isTablet } from './device';
